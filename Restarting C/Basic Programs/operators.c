#include <stdio.h>  // standard input output header file
#include <conio.h>  //console input/output -> takes input from keyboard and displays on screen

int main()
{
    int a =10,b=20;
    printf("Addition is %d",b+a);
    printf("\nSubtraction is %d",b-a);
    printf("\nMultiplication is %d",b*a);
    printf("\nDivision is %d",b/a);
    printf("\nModulus is %d",b%a);
}
